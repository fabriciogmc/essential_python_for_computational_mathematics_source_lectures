# Basic function definition


def square_area(side_length):
    """
    This function computes the area of a square
    with side equal to side_length
    """
    
    return side_length**2

print(square_area(3))
print(type(square_area(3)))
